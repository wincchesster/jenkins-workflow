version: '3.9'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - 8080:8080
      - 50000:50000
    volumes:
      - /d/DevOps/jenkins-workflow/jenkins_home:/var/jenkins_home # ./jenkins_home:/var/jenkins_home or /d/DevOps/tmp/jenkins/jenkins_home:/var/jenkins_home (macOS or Windows)

  # jenkins-agent:
  #   image: 
  #   command: command
    

